#palindrome
def palindrome(x):
    l = 0
    r = len(x) - 1
    while l < r:
        if x[l] != x[r]:
            return "not a palindrome"
        l += 1
        r -= 1
        return "palindrome"
print(palindrome(input()))

